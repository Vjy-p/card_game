ALTER TABLE players
ADD CONSTRAINT players_card_count_check
CHECK(card_count >= 0);

ALTER TABLE rooms
ADD CONSTRAINT timeout_positive
CHECK(timeout_seconds > 0);

ALTER TABLE players
ADD CONSTRAINT unique_room_user
UNIQUE(room_id,user_id);

ALTER TABLE players
ADD CONSTRAINT unique_room_seat
UNIQUE(room_id,seat_index);